# LandSignal durable data (ATTOM IQ reserve, etc.)
# attom_enrichment.json is created at runtime when a live ATTOM key presents fields.
# After key expiry, set ATTOM_DATA_MODE=memory — reserved IQ still serves.
