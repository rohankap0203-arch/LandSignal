{
  "name": "LandSignal",
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq python3.12-venv >/dev/null 2>&1 || true\ncd apps/web && npm ci\ncd apps/api && python3 -m venv .venv && .venv/bin/pip install -U pip -q && .venv/bin/pip install -e '.[dev]' -q",
  "start": "bash scripts/dev-servers.sh",
  "ports": [
    { "name": "web", "port": 3000 },
    { "name": "api", "port": 8000 },
    { "name": "web-alias", "port": 51866 }
  ],
  "terminals": [
    {
      "name": "web",
      "command": "cd apps/web && npm run dev -- -H 0.0.0.0 -p 3000",
      "description": "Next.js web UI — Cursor auto-forwards port 3000 into the right-side browser"
    },
    {
      "name": "api",
      "command": "cd apps/api && set -a && [ -f .env ] && . ./.env; set +a && AUTO_DISCOVER_ON_STARTUP=false LAND_ALERTS_MONITOR_ENABLED=false .venv/bin/uvicorn landsignal.main:app --host 0.0.0.0 --port 8000",
      "description": "API behind Next /v1 rewrite on port 8000"
    }
  ]
}
